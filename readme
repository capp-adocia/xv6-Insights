# xv6源码读前感想
- 关于为什么我要读xv6，原因其实很简单就是我在自己开发OS(TuixOS)遇到了令人受挫的bug，这非常不幸，我调试了将近一个月也没什么头绪，这也许是前期工作存在着什么纰漏，为此，我必须精读xv6源码，实话说，xv6源码也可算人类历史上巨大的宝藏，不好好加以利用怎么能行呢。
- 还有一点就是，当时的我是为了读源码而去读源码，这是一种极其错误的learning方法，这有点类似于为了做产品而去产品这种思路，相比之下，我更推崇的是一种做法，就是任务驱动，比如我今天写OS出问题了，这样我去仔细研读看看其他人怎么写的，思路如何，这样更有驱动力，我很庆幸自己已经意识到了很多不足，现在就交给时间去解决吧。
- ps：以下我写的并不一定都对，我会按照自己的理解尽可能地写对，若有错误可以在`讨论`中指出，非常感想您！

# xv6-前言
操作系统，恐怕是人类开发过最复杂最常用的软件了，刚开始有os时，还没有软件工程这一学科，应该是六七十年代人们编写os时逐渐形成的吧，因为os是一个庞大的软件，动一处而牵连其他地方跟着改变，这在软件工程中叫做耦合，我记得看过的一本书讲的飞行员与直升机控制的故事，形象地显现出耦合的弊端，平常软件开发都要求低耦合高内聚，os更是如此。

# xv6-cpu
xv6兼容哪些cpu，我不太清楚，但80386 cpu它肯定是兼容的，恰好我开发我的os也是以i386为基准开发，它是第一个使用虚拟内存的cpu，用来二级分页来扩展和保护地址空间，也是第一个32位的cpu，8086是16位的，80286是24位的，马上80386就换成的32位的，可能考虑16和24都不太够用吧，到了八九十年代人们觉得32位也不太够用，于是开始着力开发64位cpu，但为了保留兼容性，x86_64牺牲太多了，回想起来64位也才是21世纪初开始流行起来的，多线程大概在零几年出现在家用cpu上了，不得不感叹计算机发展迅速，50年前被我戏称为上古年代，现在想来挺有趣的。

# xv6-启动阶段

## boot启动

1. 先看bootasm.S文件，这个文件是计算机上电加载的第一个代码，bios会从磁盘的0柱面0磁道1扇区读取这个代码，然后运行到物理地址0x7c000处，刚开始这个其实处在实模式又叫实地址模式即只能使用16位的地址空间，这个看起来让人一头雾水，有人可能会疑问为什么直接加载32位呢，这其实算是历史遗留问题，由于8086开创了先河，很多机器上启动都要兼容8086，那么没有办法，后续开发的cpu都是向前兼容的，于是现代的计算机启动都无法避免先进入实模式，再进入保护模式（32位/64位），虽然很麻烦，但这是开发os必经之路，容不得半点马虎，所以这份代码就是先从实模式->保护模式，随后调用bootmain.c中的bootmain()。这里从实模式跳转到保护模式还有一些必备工作要做，第一步先关中断，防止被bios中断干扰跳转；第二步开启A20,这个也是历史遗留问题，感兴趣的可以查查wiki；第三步就是加载gdt描述符，这个gdt表是预先在数据段中做的也可以边执行边做，前一种更简单方便，xv6就是前者，gdt比较复杂字段比较多，不一一说明了，简单说说吧，从分段和分页寻址方式讲起吧，分段模式，这个主要用于实模式，现代保护模式几乎不用了，都在用分页，分段寻址有cs和ip，cs存放段寄存器，而ip就是相对偏移量，比如cs=0x1000,ip=3;那么最后的地址就是0x10003,这样尽管看上去只能访问16位空间，实际能访问的20位，而分页方式本人还在学习，讲不了什么，这里我将这两种方式你可能以为实模式是用分段，跳转到保护模式用分页了，其实不然，跳转后还是分段，只不够现在的分段比起之前操作好了太多了，不用再显式地设置cs，ds等这些段的内容了，这里就是gdt表发挥的作用了，把gdt表看成是一个结构体数组，结构体中指明了该段是代码段还是数据段，它的地址又是从哪里到哪里到哪里，权限又是什么等等设置，这些信息为我们以后分页做好了准备。xv6这里就设置了3个表项，第一个默认为null，后面两个就是代码段和数据段了，还有一个gdt描述符，这其实就是指向gdt表的结构体，存了gdt表地址和界限，然后我们才汇编指令中使用lgdt这个命令来加载这个gdt描述符，从而得到这些表项的信息，对cpu来说这就是一个地图，标清了各个”藏宝点“的位置等信息。这还不算完，最后设置cr0保护模式使能位为1,再跳转到32位的代码这才完成了从实模式到保护模式的飞跃。

2. bootmain.c文件是用来读取c内核的，为什么要现在读，不在实模式读，这里就要提到实模式的限制了，实模式下只能访问16位的地址空间，尽管实际可以利用偏移4位来访问20位的空间，但不够自由，而xv6似乎将用户空间放在地址，而内核放在高地址处0x80000000,在后面我们就能看到了，bootmain()解析存储在磁盘上的os内核文件，这个文件称为elf文件，可以简单类比为windows上的exe文件，exe文件有相应的文件头来标明当前文件类型，同样，elf也有，所以需要读入这个文件检查是否符合我们想要的elf文件格式，然后读取下一个运行的函数地址，跳转执行，在这里应该跳转到entry.S这个入口函数了。

3. 看entry.S这个文件所在的目录，你很容易找到下面和它很像的文件名为entryother.S这个文件，凭我们的直觉它是多余的吗？这个问题在我们继续研读下去，将会揭晓答案。entry.S设置了一些寄存器，比如cr0、cr4等等，这些是初始化它们的状态的，这里xv6是提前分页了，所以要设置这些标志位，最后跳转到main.c的入口函数，此时我们终于不用再看令人抓狂的汇编的，尽管接触汇编快有半年多了，到现在还是看着难受，还是c香。话不多说了，让我们看看c内核咋写的吧，相信比起之前要好读不少哈。最后吐槽一句，xv6的c代码风格是真不适应，写个函数还把返回类型和函数名换行分开，看的着实别扭。

## xv6-进入c内核

1. main.c

